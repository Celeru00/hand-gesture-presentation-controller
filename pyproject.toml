[project]
name = "hand-gesture-presentation-controller"
version = "0.1.0"
description = "Real-time hand gesture recognition system for controlling Microsoft PowerPoint"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "Pillow",
    "pyautogui",
    "opencv-python",
    "mediapipe==0.10.18",
]
